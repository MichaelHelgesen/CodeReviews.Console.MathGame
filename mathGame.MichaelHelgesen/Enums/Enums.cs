namespace mathGame.MichaelHelgesen.Enums;

enum GameType
{
    Addition,
    Multiplication,
    Division,
    Subtraction,
    Random
}

enum Difficulty
{
    Easy,
    Normal,
    Hard
}

enum GameChoice
{
    Play,
    Results,
    Difficulty,
    Quit
}