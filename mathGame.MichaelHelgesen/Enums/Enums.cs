namespace mathGame.MichaelHelgesen.Enums;

enum GameType
{
    Addition,
    Multiplication,
    Division,
    Subtraction,
    Random,
    Back
}

enum Difficulty
{
    Easy,
    Normal,
    Hard,
    Back
}

enum GameChoice
{
    Play,
    Results,
    Difficulty,
    Quit
}